services:
  personal-memory-system:
    build:
      context: .
    image: personal-memory-system:latest
    ports:
      - "3030:3030"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
